# OmniQL for Python

A standardized, multi-database query language (OQL) for Python.

## Installation

```bash
pip install omniql
```

## Usage

```python
import asyncio
from omniql import OmniEngine, Query

async def main():
    engine = OmniEngine()
    # ...
```
